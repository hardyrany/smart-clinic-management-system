spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clinic?useSSL=false&serverTimezone=UTC
    username: root
    password: tarrafal92
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate      
    show-sql: true
    open-in-view: false         
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect 

  data:
    mongodb:
      uri: mongodb://localhost:27017/clinicdb

server:
  port: 8080

jwt:
  secret: our-very-long-secret-key-minimum-32-characters-for-security
  expiration: 3600000